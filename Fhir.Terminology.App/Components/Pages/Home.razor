@page "/"

<h1>FHIR Terminology Service</h1>
<p>REST 基底路徑：<code>/fhir</code>（例如 <code>GET /fhir/metadata</code>）。</p>
<p><strong><a href="/docs">使用說明（線上手冊）</a></strong> — 解析順序、操作表、管理 API、組態與錯誤處理。</p>
<p>管理：<a href="/admin/import"><strong>匯入資源</strong></a>（FHIR JSON）、<a href="/admin/resources">資源列表</a>、<a href="/admin/bindings">綁定</a>、<a href="/admin/upstreams">上游</a>。</p>
<p><strong>OpenAPI／Scalar</strong>：<a href="/scalar/v1" target="_blank" rel="noopener noreferrer">互動式 REST 文件</a>（各端點附中文說明；預設開啟；正式環境可設定 <code>Terminology:EnableApiReference=false</code> 關閉）。規格 JSON：<code>/openapi/v1.json</code>。</p>
