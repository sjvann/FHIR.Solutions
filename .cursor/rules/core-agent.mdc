---
description: 全專案通用 — 溝通語言、回覆品質、差異範圍與引用格式
alwaysApply: true
---

# 核心協作規範

## 語言

- 與使用者說明、回覆一律使用**繁體中文**（技術名詞可保留英文）。
- 程式碼識別字、公開 API 命名維持英文；註解與 XML 文件與**同目錄既有檔案**語氣一致（可中英並行）。

## 變更範圍

- 只改與任務直接相關的程式與設定；**禁止**順手重構、無關格式化、或擴張需求以外的檔案。
- 除非使用者要求，**不要**新增或修改 README／設計文件。

## 引用程式碼

- 引用既有程式碼時使用工作區要求的格式：開頭 \`\`\` 獨立成行，並標註 \`startLine:endLine:filepath\`。
- 長區塊可用註解略過無關片段，避免塞滿無用行數。

## 執行與除錯

- 在可安全執行的環境下，優先**實際執行指令或工具**驗證，而非只給使用者操作步驟。
- 失敗時嘗試替代做法或診斷，避免一次失敗就放棄。
